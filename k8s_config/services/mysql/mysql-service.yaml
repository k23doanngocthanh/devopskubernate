apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: default
spec:
  selector:
    app: mysql
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
# apiVersion: v1
# kind: Service
# metadata:
#   name: mysql-service
#   namespace: default
# spec:
#   ports:
#     - port: 3306
#       targetPort: 3306  
#   selector:
#     app: mysql
#   type: LoadBalancer

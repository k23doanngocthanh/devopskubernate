apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin-service
  namespace: default
spec:
  selector:
    app: phpmyadmin-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP